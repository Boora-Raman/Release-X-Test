apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: release-x
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      name: backend
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: booraraman/wanderlust-backend-beta:aa3b7fa94ea3fbd2452b6603462e1a0c2d50495c
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: release-x
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 31100
